@model Warehouse.Models.DTOs.ProductDto
@{
    ViewData["Title"] = "Delete Product";
}

<div class="d-flex justify-content-between align-items-center mb-4 pb-2 border-bottom">
    <div>
        <h1 class="page-title mb-1 text-danger">Delete Product</h1>
        <p class="text-muted mb-0">Confirm deletion of catalog product @Model.SKU</p>
    </div>
    <div>
        <a asp-action="Index" class="btn btn-secondary">
            <i data-lucide="arrow-left" style="width: 16px; height: 16px;"></i> Cancel
        </a>
    </div>
</div>

<div class="row">
    <div class="col-lg-6">
        <div class="alert alert-danger mb-4" role="alert">
            <div class="d-flex align-items-center gap-2">
                <i data-lucide="alert-triangle" style="width: 20px; height: 20px; color: #DC2626; flex-shrink: 0;"></i>
                <span><strong>CRITICAL WARNING:</strong> Are you sure you want to delete this product? This action is permanent and cannot be undone.</span>
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-header">
                <span class="fw-semibold text-danger">Product Details to Delete</span>
            </div>
            <div class="card-body p-0">
                <table class="table mb-0">
                    <tbody>
                        <tr>
                            <td class="fw-semibold text-muted" style="width: 35%; background-color: #F8FAFC;">Product Name</td>
                            <td><strong>@Model.Name</strong></td>
                        </tr>
                        <tr>
                            <td class="fw-semibold text-muted" style="background-color: #F8FAFC;">SKU Code</td>
                            <td>
                                <span class="badge text-secondary" style="background-color: #F1F5F9; color: #475569 !important; border: 1px solid #E2E8F0;">
                                    @Model.SKU
                                </span>
                            </td>
                        </tr>
                        <tr>
                            <td class="fw-semibold text-muted" style="background-color: #F8FAFC;">Category</td>
                            <td>@(Model.CategoryName ?? "—")</td>
                        </tr>
                        <tr>
                            <td class="fw-semibold text-muted" style="background-color: #F8FAFC;">Unit Price</td>
                            <td class="fw-bold text-primary">@Model.UnitPrice.ToString("C")</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
            @Html.AntiForgeryToken()
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-danger">
                    <i data-lucide="trash-2" style="width: 16px; height: 16px;"></i> Confirm Delete
                </button>
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            lucide.createIcons();
        });
    </script>
}
